name: CI — Build and Scan

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-scan:
    runs-on: ubuntu-latest

    steps:
      # download the repo code to the runner
      - name: Checkout code
        uses: actions/checkout@v4

      # set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # build the Docker image locally (no push)
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: false
          tags: todo-backend:latest
          load: true

      # scan the image with Trivy
      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: todo-backend:latest
          format: table
          severity: CRITICAL,HIGH
          exit-code: 1

  CI-status:
    needs: [build-and-scan]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - run: |
          if [[ "${{ needs.build-and-scan.result }}" == "skipped" ]]; then
            echo "CI skipped"
          elif [[ "${{ needs.build-and-scan.result }}" == "success" ]]; then
            echo "CI passed"
          else
            echo "CI failed"
            exit 1
          fi